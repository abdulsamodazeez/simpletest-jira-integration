# Copy to .env for default JIRA credentials (optional — the Streamlit sidebar
# can override these per browser session after *Apply connection*).

JIRA_BASE_URL=https://your-site.atlassian.net
JIRA_EMAIL=you@company.com
JIRA_API_TOKEN=your-api-token-here
