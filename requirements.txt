# Streamlit >=1.50 pulls Pillow with manylinux wheels for Python 3.14 (Cloud).
# Older Streamlit + Pillow 10.4 forces a source build on 3.14 and fails without zlib headers.
streamlit==1.57.0
requests==2.32.3
python-dotenv==1.0.1
